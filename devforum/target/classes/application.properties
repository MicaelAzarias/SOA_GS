# Nome da Aplica��o
spring.application.name=DevForum

# Configura��o do Banco de Dados MySQL
# Crie um banco de dados chamado 'dev_forum' ou altere o nome abaixo
spring.datasource.url=jdbc:mysql://localhost/dev_forum?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=@F1lh0t2s

# Configura��o do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configura��o do Flyway
spring.flyway.baseline-on-migrate=true

# Configura��o de E-mail (substitua pelas suas credenciais ou use vari�veis de ambiente)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Email de origem
devforum.mail.from=nao-responda@devforum.com
devforum.mail.sender-name=DevForum - Plataforma de D�vidas